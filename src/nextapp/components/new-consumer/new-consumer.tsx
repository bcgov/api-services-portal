import * as React from 'react';
import { Button } from '@chakra-ui/react';

const NewConsumer: React.FC = () => {
  return (
    <>
      <Button variant="primary">Create Consumer</Button>
    </>
  );
};

export default NewConsumer;
